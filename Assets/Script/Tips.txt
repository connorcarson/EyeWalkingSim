--------------pick up ---------------
all the things that can be picked up need rigidbody
and collider
Collision Detection set to continuous
and tag as canBePickup
audiosource on the pickup item will play when the item is picked up
should add the script (itembeenpickedup)
build a PickupPosition as a child of the cam, and assign this cam to the script(attach to the cam)

--------------quest--------------
for quest item,each of them has a child called Text
pharmacydoctorandtape should be on the doctor
1.sound
the FollowQuest controller need mumble sound and audiosource
each massobj need AS and clip
doctor's have their own AS and clip